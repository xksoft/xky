<UserControl x:Class="Xky.Core.UserControl.MyTagLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Style>
        <Style TargetType="{x:Type UserControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type UserControl}">
                        <Border CornerRadius="8" Background="{Binding Path=TabLabelBackground}"
                                VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Path=TabLabelText}"
                                       Foreground="{Binding Path=TabLabelForeground}" VerticalAlignment="Center"
                                       HorizontalAlignment="Center" Margin="5,3,5,3" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>


            </Setter>
        </Style>

    </UserControl.Style>


</UserControl>