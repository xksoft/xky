<UserControl x:Class="Xky.Platform.UserControl.MyProgressbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="uc"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border CornerRadius="5" Background="Transparent">
            <Border ClipToBounds="True" CornerRadius="5" BorderBrush="{Binding BorderBrush}" BorderThickness="{Binding BorderThickness}">
                <Grid>

                    <Rectangle  Width="{Binding ProgressBarWidth, ElementName=uc}" HorizontalAlignment="Left" Fill="{Binding FForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"></Rectangle>
                    <!--<Grid HorizontalAlignment="Left" Background="{Binding aaa}">-->
                    <!--<Grid Width="{Binding ProgressBarWidth, ElementName=uc}" HorizontalAlignment="Left" >
                        <Grid.Background>
                            <SolidColorBrush Color="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"></SolidColorBrush>
                        </Grid.Background>
                    </Grid>-->
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
